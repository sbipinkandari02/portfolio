import ProjectsContainer from "@/components/sections/Project";

export const metadata = {
  title: "Projects | Bipin Kandari",
  description:
    "Real-world projects built using React, Next.js, Node.js, and MongoDB.",
};

const ProjectPage = () => {
  return <ProjectsContainer />;
};

export default ProjectPage;
